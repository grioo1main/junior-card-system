# 🏦 Junior Card Management System

REST API для управления детскими банковскими картами. Родители могут создавать карты для детей, управлять их статусами и контролировать доступ.

---
> **📚 Учебный проект** - создан для демонстрации навыков работы со Spring Boot, REST API и базами данных. Бизнес-логика упрощена для фокуса на технической реализации.
## 🛠️ Технологии

- **Java 21** - основной язык разработки
- **Spring Boot 4** - фреймворк для REST API
- **Spring Data JPA** - работа с базой данных
- **PostgreSQL** - реляционная БД
- **Hibernate 6** - ORM маппинг
- **JUnit 5 + Mockito** - тестирование
- **Maven** - сборка проекта
- **Lombok** - уменьшение boilerplate кода

---

## 💼 Бизнес-логика

### Родительская карта
- Владелец должен быть **от 18 до 99 лет**
- При создании карта получает статус **PENDING** (ожидает активации)
- Может иметь **максимум 3 детские карты**
- Нельзя удалить родителя, пока есть привязанные детские карты
- Статусы: PENDING → ACTIVE → SUSPENDED/EXPIRED/CANCELLED

### Детская карта
- Ребенок должен быть **от 6 до 17 лет**
- Обязательно привязывается к родительской карте
- При создании проверяется лимит (не более 3 карт у родителя)
- Статусы управляются с проверкой валидных переходов
- Можно обновить имя и возраст

### Управление статусами
Система контролирует переходы между статусами:

| Текущий статус | Разрешённые переходы |
|----------------|---------------------|
| PENDING | ACTIVE, CANCELLED |
| ACTIVE | SUSPENDED, EXPIRED, CANCELLED |
| SUSPENDED | ACTIVE, CANCELLED |
| EXPIRED | Финальный (нельзя изменить) |
| CANCELLED | Финальный (нельзя изменить) |

### Валидация данных
- **Имя**: 2-15 символов
- **Возраст родителя**: 18-99 лет
- **Возраст ребенка**: 6-17 лет
- **Лимит карт**: максимум 3 на родителя

---

## 📡 API Endpoints

### Родительские карты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/cards/parent` | Создать родительскую карту |
| GET | `/api/cards/parent` | Получить все родительские карты |
| GET | `/api/cards/parent/{id}` | Получить родителя по ID |
| PUT | `/api/cards/parent/{id}` | Обновить данные родителя |
| PUT | `/api/cards/parent/{id}/status` | Изменить статус карты |
| DELETE | `/api/cards/parent/{id}` | Удалить родителя (только без детей) |

### Детские карты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/cards/junior` | Создать детскую карту |
| GET | `/api/cards/junior` | Получить все детские карты |
| GET | `/api/cards/junior/{id}` | Получить карту по ID |
| PUT | `/api/cards/junior/{id}` | Обновить данные карты |
| PUT | `/api/cards/junior/{id}/status` | Изменить статус карты |
| DELETE | `/api/cards/junior/{id}` | Удалить карту |

---

## ⚠️ Обработка ошибок

| Код | Ошибка | Описание |
|-----|--------|----------|
| 404 | CARD_NOT_FOUND | Карта или родитель не найдены |
| 409 | ACCOUNT_LIMIT_EXCEEDED | Превышен лимит (максимум 3 карты) |
| 400 | VALIDATION_ERROR | Невалидные данные (возраст, имя) |
| 400 | INVALID_STATUS_TRANSITION | Невалидный переход статуса |
| 400 | PARENT_HAS_CHILDREN | У родителя есть активные детские карты |

---

## 🏗️ Архитектура

Проект построен по трехслойной архитектуре:

- **Controller** - обработка HTTP запросов, валидация
- **Service** - бизнес-логика, транзакции, проверка правил
- **Repository** - работа с базой данных через JPA
- **DTO** - передача данных между слоями
- **Exception Handler** - централизованная обработка ошибок

---

## 🧪 Тестирование

- **Unit-тесты** (JUnit 5 + Mockito) - покрытие бизнес-логики
- **Integration-тесты** (MockMvc) - тестирование API endpoints
- Общее покрытие: ~80%

---

## 🚀 Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/grioo1main/junior-card-system.git
cd junior-card-system

# Настроить application.properties (БД PostgreSQL)

# Запустить
mvnw spring-boot:run
